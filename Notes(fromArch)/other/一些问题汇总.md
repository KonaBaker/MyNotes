## 问题汇总－fft部分

### 1.对于波浪谱生成部分，在oceanography.glsl文件中发现和论文中的公式出入很大，具体：如果学长有其他具体参考出处(之前学长你给的fft资料论文中也没有以下有出入的地方的明确公式: ( )，感谢指明　: )

- 方向拓展函数：

```
float direction_spectrum(float theta, float omega, float peak_omega, Spectrum_Parameters pars)
{
    //float spread = Donelan_Banner(angle, omega, peak_omega);

    float s = spread_power_hasselman(omega, peak_omega, pars.wind_speed)
            + mix(16 * tanh(min(omega / peak_omega / 10, 20)), 25, pars.extra_alignment) * pars.extra_alignment * pars.extra_alignment;
    float spread = cosine2s(theta, s);

    return mix(0.5 / PI, spread, pars.alignment);
}
```

和论文中的公式对应不起来。尤其是后面alignment还有这些mix函数。

#### 答：



- jonswap：

```
return JONSWAP_alpha(chi) * G * G
        * one_over_omega * one_over_omega * one_over_omega * one_over_omega * one_over_omega
        * exp(-1.25 * peak_omega_over_omega * peak_omega_over_omega * peak_omega_over_omega * peak_omega_over_omega)
        * pow(abs(gamma), r) * 3.3 / gamma;
```

论文中的gamma参数是个常数值3.3，可是在这里作为控制波浪陡峭参数可变，且外面又乘了个3.3

#### 答：

- tma_correction:

论文中又是sinh又是tanh的，这里咋这么简单：

```
loat TMA_correction(float omega, float depth)
{
    float omegaH = omega * sqrt(depth / G);
    if (omegaH <= 1)
        return 0.5 * omegaH * omegaH;
    else if (omegaH < 2)
        return 1.0 - 0.5 * (2.0 - omegaH) * (2.0 - omegaH);
    else
        return 1;
}
```



#### 答：

- 还有就是hasselman方法：

```
float spread_power_hasselman(float omega, float peak_omega, float u)
{
    if (omega > peak_omega)
    {
        return 9.77 * pow(abs(omega / peak_omega), -2.33 - 1.45 * (u * peak_omega / G - 1.17));
    }
    else
    {
        return 6.97 * pow(abs(omega / peak_omega), 4.06);
    }
}
```

-2.33 - 1.45 * (u * peak_omega / G - 1.17));这里论文中也没有出现。



#### 答：

- frequency

并不是w=根号下gk

```
float frequency(float k, float depth)
{
    //return sqrt(G * k * tanh(min(k * depth, 20)));
    return sqrt((G * k + sigma_over_rho * k * k * k) * tanh(min(k * depth, 10)));
}
```



#### 答：

### 2.oceanography中frequency_derivative和short_waves_fade是干什么的

```
float short_waves_fade(float kLength, float fadeLength)
{
    return exp(-fadeLength * fadeLength * kLength * kLength);
}
```

```
float frequency_derivative(float k, float depth)
{
    float th = tanh(min(k * depth, 10));
    float freq = frequency(k, depth);
    float ch = cosh(min(k * depth, 10));

    //return G * (depth * k / ch / ch + th) / freq / 2;
    return (depth * (sigma_over_rho * k * k * k + G * k) / ch / ch
        + (G + 3 * sigma_over_rho * k * k) * th) * 0.5 / freq;
}
```



#### 答：

### 3.在初始化波浪谱的时候，为啥要去除高频和低频信号cutoff_high＆cutoff_low？和cascade的关系？（如果可以稍微详细讲解一下这个cascade更好了（或者有哪些资料），论文中提到的比较少）

#### 答：

### 4.输出turbulence,derivative,displacement

答：

### 5.带或者不带looped的前缀的idft与SS_EXT_WATER_SYSTEM_IDFT_COMPUTE_TYPE有关，是不是可以认为带looped性能好，不带looped可以被认为基本不再使用？

答：


**thread local**

存储期限定符。例如static\extern.

只能声明于命名空间作用域对象、块作用域对象以及静态数据成员。

看起来是全局或者静态存储器的变量，但是它每个线程都有一个备份。

在线程创建时产生，在线程结束时销毁。（注意：**其是绑定线程的**，只有第一次声明的时候被赋值。但是其作用域仍保持在声明的作用域内）。

> 类似于thread的static变量，一般要求我们在第一次声明的时候赋值，否则会多次被赋值。



**static**

只会被初始化一次并在程序退出时销毁。

静态存储。在作用域之外也能保持值。

**extern**

